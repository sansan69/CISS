Deploy CISS Workforce to Vercel.

Steps:
1. Run `npm run typecheck` to verify no type errors
2. Run `npm run lint` to check for lint issues
3. Run `npm run build` to verify production build succeeds
4. If all pass, push to `main` branch (Vercel auto-deploys)
5. Check Vercel deployment status

Verify: build succeeds, deployment is live on Vercel.