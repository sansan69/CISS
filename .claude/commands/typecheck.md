Run TypeScript type checking on the CISS project.

Run: `npx tsc --noEmit -p tsconfig.typecheck.json`

Report:
- If errors: list each error with file path and line number
- If clean: confirm zero errors

Do not fix errors unless asked. Just report.